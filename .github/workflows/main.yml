name: Frontend CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  check-frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Check files
        run: echo "Frontend files are present and OK"